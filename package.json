{
  "name": "nest-hexa-services",
  "version": "1.0.0",
  "description": "CLI para generar módulos NestJS con arquitectura hexagonal o por capas",
  "type": "module",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "nestmod": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run tsc -p tsconfig.json",
    "prepare": "bun run build"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "kleur": "^4.1.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
